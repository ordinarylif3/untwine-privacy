# Untwine — privacy policy & support pages

Public pages for the Untwine puzzle game, served by GitHub Pages:

- Privacy Policy: `index.html`
- Support: `support.html`

The game itself lives in a private repo; this repo exists only so the store
listings have a public URL. Keep `index.html` in sync with the `privacy.html`
bundled in the app.
